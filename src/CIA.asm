DATA SEGMENT
DATA ENDS

STACK SEGMENT
DB 100 DUP(?)
STACK ENDS

CODE SEGMENT
ASSUME CS:CODE,DS:DATA,SS:STACK

START PROC
MOV     AX,DATA
MOV     DS,AX
MOV     CL,80H
CICL: 
MOV     SI,82H
MOV     CX,1
MOV     AL,[SI]
CMP     AL,'.'
JE      PAR
PUSH    SI
ADD     SI,CX
JMP     CICL
START ENDP

CODE ENDS
END START
